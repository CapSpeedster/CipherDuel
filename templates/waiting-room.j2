<html>
    <head>
        <script>
            async function connect(){
                console.log('Connecting')
                let packet = {
                    'action' : 'connection',
                    'data' : {}
                }

                let response = await fetch('', {
                    method: 'PUT',
                    headers: {
                        'Content-Type' : 'application/json'
                    },
                    body: JSON.stringify(packet)
                })
            }

            window.onload = connect;
        </script>
    </head>
    <body>
        <div>Waiting for other user. MatchID: {{matchID}}</div>
    </body>
</html>